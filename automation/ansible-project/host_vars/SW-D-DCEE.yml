# ============================================================
# host_vars/SW-D-DCEE.yml — Distribution Switch for DCEE
# ============================================================
# Topology connections (from GNS3 & trunk_config.md):
#   Gi0/0 → SW-A-DCEE (Gi0/0)   [trunk downlink]
#
# Note: Uplink to SW-CORE (Gi0/2) is converted to a Layer 3
#       routed port (10.0.20.2/30) per L3 conversion report.
# ============================================================

trunk_interfaces:
  - name: GigabitEthernet0/0
    description: TRUNK_TO_SW-A-DCEE
    native_vlan: 100
    allowed_vlans: "10,20,30,40,99,100"

# No access ports on distribution switches
access_interfaces: []
